# user-purchases

Користувацькі маршрути з `src/modules/user-modules/user-purchases/user-purchases.controller.ts`.

Функції — у `user-purchases.api.ts`, параметри й відповіді — у `user-purchases.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findMy` | GET | `/user-purchases/my` |
