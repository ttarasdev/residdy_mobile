# case-reminders

Користувацькі маршрути з `src/modules/cases-modules/case-reminders/case-reminders.controller.ts`.

Функції — у `case-reminders.api.ts`, параметри й відповіді — у `case-reminders.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findAll` | GET | `/case-reminders` |
| `findOne` | GET | `/case-reminders/:id` |
