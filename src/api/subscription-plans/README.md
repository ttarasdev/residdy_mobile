# subscription-plans

Користувацькі маршрути з `src/modules/subscription-modules/subscription-plans/subscription-plans.controller.ts`.

Функції — у `subscription-plans.api.ts`, параметри й відповіді — у `subscription-plans.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `list` | GET | `/subscription-plans` |
| `get` | GET | `/subscription-plans/:id` |
