# case-instructions

Користувацькі маршрути з `src/modules/cases-modules/case-instructions/case-instructions.controller.ts`.

Функції — у `case-instructions.api.ts`, параметри й відповіді — у `case-instructions.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `getAll` | GET | `/case-instructions` |
| `findOne` | GET | `/case-instructions/:id` |
