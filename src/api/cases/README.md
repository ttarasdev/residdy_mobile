# cases

Користувацькі маршрути з `src/modules/cases-modules/cases/cases.controller.ts`.

Функції — у `cases.api.ts`, параметри й відповіді — у `cases.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findAll` | GET | `/cases` |
| `getOne` | GET | `/cases/:id` |
