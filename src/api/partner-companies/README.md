# partner-companies

Користувацькі маршрути з `src/modules/partners-modules/partner-companies/partner-companies.controller.ts`.

Функції — у `partner-companies.api.ts`, параметри й відповіді — у `partner-companies.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `getAll` | GET | `/partner-companies` |
| `getOne` | GET | `/partner-companies/:id` |
