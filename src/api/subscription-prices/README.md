# subscription-prices

Користувацькі маршрути з `src/modules/subscription-modules/subscription-prices/subscription-prices.controller.ts`.

Функції — у `subscription-prices.api.ts`, параметри й відповіді — у `subscription-prices.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `list` | GET | `/subscription-prices` |
| `get` | GET | `/subscription-prices/:id` |
