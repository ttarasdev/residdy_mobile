# consultation-categories

Користувацькі маршрути з `src/modules/consultation-modules/consultation-categories/consultation-categories.controller.ts`.

Функції — у `consultation-categories.api.ts`, параметри й відповіді — у `consultation-categories.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findActive` | GET | `/consultation-categories/active` |
| `findOne` | GET | `/consultation-categories/:id` |
