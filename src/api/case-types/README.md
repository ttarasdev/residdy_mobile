# case-types

Користувацькі маршрути з `src/modules/cases-modules/case-types/case-types.controller.ts`.

Функції — у `case-types.api.ts`, параметри й відповіді — у `case-types.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findAll` | GET | `/case-types` |
| `findOne` | GET | `/case-types/:id` |
