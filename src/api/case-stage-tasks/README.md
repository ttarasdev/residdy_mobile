# case-stage-tasks

Користувацькі маршрути з `src/modules/cases-modules/case-stage-tasks/case-stage-tasks.controller.ts`.

Функції — у `case-stage-tasks.api.ts`, параметри й відповіді — у `case-stage-tasks.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findByStage` | GET | `/case-stage-tasks/by-stage/:stageId` |
