# public-assets

Користувацькі маршрути з `src/modules/media-modules/public-assets/public-assets.controller.ts`.

Функції — у `public-assets.api.ts`, параметри й відповіді — у `public-assets.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `list` | GET | `/public-assets` |
| `getById` | GET | `/public-assets/:id` |
