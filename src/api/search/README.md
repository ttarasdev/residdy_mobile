# search

Користувацькі маршрути з `src/modules/search/search.controller.ts`.

Функції — у `search.api.ts`, параметри й відповіді — у `search.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `search` | GET | `/search` |
