# consultation-slots

Користувацькі маршрути з `src/modules/consultation-modules/consultation-slots/consultation-slots.controller.ts`.

Функції — у `consultation-slots.api.ts`, параметри й відповіді — у `consultation-slots.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findPublic` | GET | `/consultation-slots` |
