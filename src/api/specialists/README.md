# specialists

Користувацькі маршрути з `src/modules/specialist-modules/specialists/specialists.controller.ts`.

Функції — у `specialists.api.ts`, параметри й відповіді — у `specialists.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `getById` | GET | `/specialists/:id` |
