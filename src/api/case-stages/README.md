# case-stages

Користувацькі маршрути з `src/modules/cases-modules/case-stages/case-stages.controller.ts`.

Функції — у `case-stages.api.ts`, параметри й відповіді — у `case-stages.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findByCase` | GET | `/case-stages/:caseId` |
