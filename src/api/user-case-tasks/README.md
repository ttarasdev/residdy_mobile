# user-case-tasks

Користувацькі маршрути з `src/modules/user-cases-modules/user-case-tasks/user-case-tasks.controller.ts`.

Функції — у `user-case-tasks.api.ts`, параметри й відповіді — у `user-case-tasks.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `toggle` | POST | `/user-case-tasks/:id/toggle` |
| `setDate` | POST | `/user-case-tasks/:id/set-date` |
