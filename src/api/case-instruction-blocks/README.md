# case-instruction-blocks

Користувацькі маршрути з `src/modules/cases-modules/case-instruction-blocks/case-instruction-blocks.controller.ts`.

Функції — у `case-instruction-blocks.api.ts`, параметри й відповіді — у `case-instruction-blocks.types.ts`.
Спільні правила, приклади та помилки: [API README](../README.md).

| Функція | HTTP | Маршрут |
|---|---|---|
| `findByInstruction` | GET | `/case-instruction-blocks/:instructionId` |
